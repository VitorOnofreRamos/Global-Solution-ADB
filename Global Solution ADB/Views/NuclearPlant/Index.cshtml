@model IEnumerable<Global_Solution_ADB.Application.DTOs.AppointmentDTO>
@{
    ViewData["Title"] = "NuclearPlants";
}

<h2 class="mt-3">@ViewData["Title"]</h2>

<div class="card">
	<div class="card-body">
		<table class="table">
			<thead>
				<tr>
					<th>ID</th>
					<th>Name</th>
					<th>FullCapacity</th>
					<th>Address</th>
					<th>NumberOfReactors</th>
				</tr>
			</thead>
			<tbody>
				@foreach (var appointmentItem in Model)
				{
					<tr>
						<td>@appointmentItem.ID</td>
						<td>@appointmentItem.Name</td>
						<td>@appointmentItem.FullCapacity</td>
						<td>@appointmentItem.Address</td>
						<td>@appointmentItem.NumberOfReactors</td>
					</tr>
				}
			</tbody>
		</table>
	</div>
</div>